import 'package:cropper_service/src/cropper_service.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  /// Delete when any test is created
  test('Mock test for CI', () {
    CropperService();
    expect(
      true,
      isTrue,
    );
  });
}
